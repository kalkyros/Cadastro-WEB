# Copie este arquivo para .env e ajuste as variáveis conforme necessário
# SECRET_KEY pode ficar vazio para desenvolvimento (será gerado automaticamente)
# FERNET_KEY é opcional; para criptografar CPF gere com: from cryptography.fernet import Fernet; Fernet.generate_key().decode()
SECRET_KEY=
DATABASE_PATH=database.db
FERNET_KEY=
FLASK_DEBUG=True
